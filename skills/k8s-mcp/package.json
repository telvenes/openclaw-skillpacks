{
  "name": "k8s-mcp",
  "version": "0.2.0",
  "description": "Read-only Kubernetes MCP server for OpenClaw (no kubectl, no deps)",
  "type": "module",
  "scripts": {
    "start": "node scripts/k8s-mcp.js"
  }
}
